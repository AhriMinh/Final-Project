import * as realTime from './realtime_function.js'
import * as buttonActive from './active-button.js'

buttonActive.acctiveButton(hero-button,button-active);

  setInterval(function() {
    realTime.updateTime()
  }, 1000);

